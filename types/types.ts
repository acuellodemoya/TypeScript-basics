
/**
 * This file content the examples of the most basic variable
 * types in typeScript and javaScript like numbers, strings,
 * booleans and arrays.
 *
 * this file is part of the course about the fundamentals of
 * TypeScript (TS)
 */

//syntax => let/const/var variableName: type = value
let num:number = 5
let str:string = 'Hello, World!'
let bool:boolean = true
const notBool:boolean = false
const numbers:number[] = [1, 2, 3, 4, 5]

console.log(`str: ${str}, num: ${num}, numbers: ${numbers}`)

